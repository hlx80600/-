# 程序总览

用普通软件结构说明：程序怎么启动、主循环干什么、一条鞋怎么从皮带走到下料。

相关文档：

| 文档 | 用途 |
|------|------|
| **本文** | 整机架构 + **主流程怎么跑** |
| [algorithm_module/readme.md](../algorithm_module/readme.md) | 视觉算法接口（输入输出、谁调用） |
| HMI「使用说明」 | 每个界面页：做什么 / 文件 / 引用 |
| [操作说明.md](操作说明.md) | 安装、联调、检查清单 |

---

## 1. 程序是干什么的

工控机上的 Python 程序，控制：

- **上料臂 robot1**（法奥 FR5）：皮带取鞋 → 放进压鞋机槽  
- **下料臂 robot2**：从槽取鞋 → 放到下料皮带  
- **四路相机** + YOLO：找鞋、对位、槽有无鞋、压杆偏移  
- **压鞋机**：四槽压合与转台  
- **双夹爪**（CAN）  

默认 `config/default.yaml` 里设备多为 **Mock**：无真机也能开界面、看步号与记忆变化。

---

## 2. 启动之后发生什么

```
python3 main.py
    │
    ├─ 读 config/default.yaml
    ├─ 创建 AppContext（机器人、夹爪、压机、相机、VisionService、gvl…）
    ├─ 创建 Coordinator（主循环线程，约 50ms 一拍）
    └─ 打开 MainWindow（HMI）+ 可选弹出「相机监控」
```

入口文件：`main.py`。

每一拍主循环（`core/coordinator.py`）大致做：

1. 把 HMI 按钮状态同步到内部运行标志（Running / Paused / Stop / 急停等）  
2. 若在初始化：跑 `stations/init_sequence.py`  
3. **并行**调用 Station1～6 的 `cycle(ctx)`（各站自己判断能不能进、当前哪一步）  
4. 刷新三色灯、处理通信掉线等  

HMI 另有约 **100ms** 刷新界面显示（`hmi/main_window.py`），与主循环分开。

---

## 3. 核心对象（读代码时抓住这几个）

| 名字 | 文件 | 作用 |
|------|------|------|
| `ctx` / `AppContext` | `core/app_context.py` | 全局上下文：点位、机器人、视觉、压机… |
| `ctx.gvl` | `core/gvl.py` | 运行标志、各站步号 `Auto_A`、记忆 `Memory_BOOL[1..10]`、Busy |
| `Coordinator` | `core/coordinator.py` | 主循环，调用各 Station |
| `ctx.vision` | `vision/vision_service.py` | 拍照业务（取图、Mock、发监控图） |
| `algo` | `algorithm_module` | 纯算法：图→结果 |
| `ctx.robot1` / `robot2` | `devices/robot_fr5.py` | `move_j` / `move_l` → 法奥 MoveJ/MoveL |

改 **工艺步序**：改 `stations/station*.py`。  
改 **IP/点位/Mock**：改 `config/default.yaml`。

---

## 4. 一条鞋的主流程（自动模式）

互锁靠记忆位 `Mem1～10`（界面「运行监控」可勾选查看）。概念顺序如下：

```
① Station1  皮带 cam1 拍照
            → 得到 PickPose（基座坐标 + 左右脚）
            → 置相关记忆（如拍照完成）

② Station2  上料臂 Auto 取料段
            → 到取料进入点 / 上方 / 下降取料 / 夹紧 / 抬起 / 退出
            → 手爪有料

③ （需要时）Station3  放料槽 cam3 拍照：槽是否空、方向是否匹配

④ Station2  上料臂 Auto 放料段
            → 进入点 → 上方 → 接近放料点 →（鞋头 TCP）对位 → 压跟
            → 张爪 → 上方 → 退出
            → 槽侧待压合/转台相关记忆

⑤ Station6  压合（按当前放料槽）→ 旋转到位 → 推进槽号

⑥ Station4  取料槽 cam4：有无鞋；有则测压杆 XY 偏移

⑦ Station5  下料臂：取槽 → 皮带放料 → 记一件产量

然后下一只鞋继续 ①…
```

### 4.1 Station 一览

| Station | 文件 | 职责 |
|---------|------|------|
| 1 | `stations/station1_belt_photo.py` | 皮带拍照 → PickPose |
| 2 | `stations/station2_robot1.py` | 上料取料 + 放料（含鞋头对位） |
| 3 | `stations/station3_place_slot_photo.py` | 放料槽视觉 |
| 4 | `stations/station4_pick_slot_photo.py` | 取料槽视觉 + 压杆 |
| 5 | `stations/station5_robot2.py` | 下料取放 |
| 6 | `stations/station6_press_rotate.py` | 压合 + 旋转 |
| Init | `stations/init_sequence.py` | 上电初始化 |

每个 `cycle(ctx)` 典型结构：

1. 更新本站是否 Busy（有步号在跑则为忙）  
2. 停机/急停/报警 → 步号清零  
3. **进入条件**满足 → 把某条 Auto 的步号设为 10  
4. 未暂停时按当前步号 `match/case` 执行：发一次运动或等待完成，再跳下一步  

**为什么运动要「发一次」？**  
主循环每 50ms 扫一次。若每拍都发 Move，会重复下发。工程里用 `pulse_cmd`：某一步里第一次返回允许发令，之后只轮询到位。

### 4.2 Station2 放料路径（便于对照代码）

```
进入点 → 上方 → 接近放料点(工具1) → 切鞋头TCP → 对位 → 压跟 → 张爪 → 上方 → 退出
```

对位逻辑在 `stations/toe_place_assist.py`，调用 `algo.classify_toe_align`。

---

## 5. 视觉怎么接到流程上

```
工位 stations/*
    → ctx.vision.photo_* / guide_*     （VisionService：取图、Mock、监控图）
        → algo.*                         （algorithm_module：检测/分类）
            → vision/legacy_pipeline 等   （YOLO / 手眼实现）
```

| 相机 | 作用 | 主要入口 |
|------|------|----------|
| cam1 | 皮带找鞋 → 取料坐标 | `photo_belt_pick` → `algo.detect_belt_pick` |
| cam2 | 鞋头是否到位 | `guide_place_edge` / toe_place_assist → `algo.classify_toe_align` |
| cam3 | 放料槽有无鞋 | `photo_place_slot` → `algo.classify_slot_occupied` |
| cam4 | 取料槽 + 压杆偏移 | `photo_pick_slot` → classify + `measure_rod_offset_mm` |

相机 Mock 时：皮带可用 HMI「屏蔽取料」示教点；详见算法 readme。

监控窗：`visualize_module` 取流 + 可选实时推演（缓存帧调 `compute_monitor`）。

---

## 6. 运动指令

工位 / 点位页最终调用：

| 封装 | 法奥 SDK | 用途 |
|------|----------|------|
| `robot.move_j(...)` | **MoveJ** | 关节空间（建议示教时存 j1..j6） |
| `robot.move_l(...)` | **MoveL** | 直线笛卡尔 |
| 全局速度 | SetSpeed | 运行监控百分比 |

按**程序步**调速：HMI「运动参数」→ yaml `motion_steps` → `ctx.step_motion_kwargs("s2a10_30")`。

---

## 7. 记忆位 Mem1～10（概念）

用于站间握手（以运行监控勾选旁文字为准，下表为常用约定）：

| Mem | 含义 |
|-----|------|
| 1 | 皮带拍照完成 |
| 2 | 上料爪有料 |
| 3 | 放料侧/待转相关 |
| 4 | 放料槽拍照完成 |
| 5 | 下料爪有料 |
| 6 | 取料槽有料 |
| 7 | 取料槽拍照完成 |
| 8 / 9 | 左鞋 / 右鞋 |
| 10 | 特殊：如不匹配时旋转不压等 |

写记忆请用 `sync_mem(ctx, 序号, True/False)`，保证逻辑与界面一致。

---

## 8. 目录速查

```
main.py                 入口
config/default.yaml     IP、点位、Mock、运动步
core/                   主循环、gvl、报警、灯
stations/               Station1～6 + 初始化
devices/                机器人、夹爪、压机、IO
vision/                 VisionService、相机、标定
algorithm_module/       算法门面 algo
visualize_module/       相机监控
hmi/                    界面
docs/程序总览.md         本文
```

---

## 9. 建议阅读顺序

1. 本文（主流程）  
2. `algorithm_module/readme.md`（视觉接口）  
3. `stations/station1_belt_photo.py` → `station2_robot1.py`（看 `cycle` 与 `case`）  
4. `vision/vision_service.py` 里 `photo_belt_pick` 一段  
5. HMI「使用说明」按页查按钮对应文件  
6. 真机前：`docs/操作说明.md` 检查清单  

---

## 10. 改代码该动哪

| 目的 | 位置 |
|------|------|
| 改自动步序 / 互锁 | `stations/station*.py` |
| 改 IP、点位、Mock、步速 | `config/default.yaml` |
| 加/改视觉算法 API | `algorithm_module/` + `vision/legacy_pipeline.py` |
| 改界面 | `hmi/pages/` |
| 改监控取流显示 | `visualize_module/` |
